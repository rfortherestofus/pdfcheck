on:
  push:
    branches: [main]
  pull_request:

name: Run unit tests

permissions: read-all

jobs:
  test-units:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - name: Install R
        uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Install R dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::testthat, any::xml2, any::devtools, any::pandoc

      - name: Run unit tests
        run: |
          Sys.info()
          devtools::load_all()
          pandoc::pandoc_install()
          bin <- install_verapdf()
          cat("USERPROFILE: ", Sys.getenv("USERPROFILE"), "\n", sep = "")
          cat("LOCALAPPDATA: ", Sys.getenv("LOCALAPPDATA"), "\n", sep = "")
          cat("ProgramFiles: ", Sys.getenv("ProgramFiles"), "\n", sep = "")
          cat("ProgramFiles(x86): ", Sys.getenv("ProgramFiles(x86)"), "\n", sep = "")
          cat("veraPDF bin: ", bin, "\n", sep = "")
          cat("Sys.which('verapdf'): '", Sys.which("verapdf"), "'\n", sep = "")
          testthat::test_dir("tests/testthat", stop_on_failure = TRUE)
        shell: Rscript {0}
